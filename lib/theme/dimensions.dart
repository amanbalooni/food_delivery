import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;
  //Food Page Body Dimensions
  static double pageFoodBodyParentContainer = screenHeight / 2.3;
  static double pageFoodBodyChildContainer = screenHeight / 3.34;
  static double pageFoodBodyChildSubContainer = screenHeight / 6.13;

  //Width

  //Height
  static double height30 = screenHeight / 24.53;

  //icon Size
  static double iconSize16 = screenHeight / 46;

  //ListView
  static double listViewImgSize = screenWidth / 3;
  static double listViewTextSize = screenWidth / 3.6;

  //Popular Food
  static double popularFoodImgSize = screenHeight / 2.1;

  //Font Size
  static double fontSize16 = screenHeight / 46;
  static double fontSize26 = screenHeight / 28.3;
}
